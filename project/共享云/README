共享云：通过建立一个HTTP服务器

HTTP服务器实现流程：
  1.创建tcp服务端、epoll监控和线程池pool
  2.通过epoll获取已经就绪的文件描述符 --> sock
      1)若就绪的是用于监听的sock，则获取已连接的客户端，并将与之通信的套接字的文件描述符添加到epoll监控中
	  2)若就绪的是用于通信的sock，则创建一个任务（sock的文件描述符，处理数据的函数），并将任务添加任务队列中
  3.线程池从任务队列中获取一个任务，并交由一个线程处理

任务处理：
	
  
  
